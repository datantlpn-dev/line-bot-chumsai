# 📘 คู่มือติดตั้งแบบละเอียด

## 📋 เช็คลิสต์ก่อนเริ่ม

- [ ] มี Google Sheets ที่มีคอลัมน์ `chumsa`, `lat`, `lng`
- [ ] มี Google My Maps URL
- [ ] มี LINE Bot Channel (Messaging API)
- [ ] มี GitHub Account
- [ ] มี Vercel Account

---

## 🔧 ขั้นตอนที่ 1: ตั้งค่า Google Sheets API

### 1.1 สร้าง Google Cloud Project
1. ไปที่ https://console.cloud.google.com/
2. คลิก **Select a project** → **New Project**
3. ตั้งชื่อ project (เช่น "LINE-Bot-Chumsai")
4. คลิก **Create**

### 1.2 เปิด API
1. ไปที่ **APIs & Services** → **Library**
2. ค้นหาและเปิด:
   - ✅ **Google Sheets API**
   - ✅ **Google Drive API**

### 1.3 สร้าง Service Account
1. ไปที่ **APIs & Services** → **Credentials**
2. คลิก **Create Credentials** → **Service Account**
3. ตั้งชื่อ (เช่น "line-bot-service")
4. คลิก **Create and Continue**
5. เลือก Role: **Basic** → **Viewer** (หรือไม่ก็ข้ามไปได้)
6. คลิก **Done**

### 1.4 ดาวน์โหลดกุญแจ JSON
1. คลิกที่ Service Account ที่สร้าง
2. ไปที่แท็บ **Keys**
3. คลิก **Add Key** → **Create new key**
4. เลือกชนิด **JSON**
5. คลิก **Create** → ไฟล์จะดาวน์โหลดอัตโนมัติ

### 1.5 แชร์ Google Sheets
1. เปิดไฟล์ JSON ที่ดาวน์โหลด
2. คัดลอกค่า `client_email` (เช่น `xxx@xxx.iam.gserviceaccount.com`)
3. เปิด Google Sheets ของคุณ
4. คลิก **Share** → วางอีเมลที่คัดลอก
5. ตั้งสิทธิ์เป็น **Viewer**
6. คลิก **Send**

---

## 🤖 ขั้นตอนที่ 2: ตั้งค่า LINE Bot

### 2.1 สร้าง Provider
1. ไปที่ https://developers.line.biz/
2. ล็อกอินด้วย LINE Account
3. คลิก **Create** → **Create a new provider**
4. ตั้งชื่อ Provider (เช่น "My Company")

### 2.2 สร้าง Messaging API Channel
1. ใน Provider ที่สร้าง คลิก **Create a Messaging API channel**
2. กรอกข้อมูล:
   - **Channel name**: ชื่อบอท (เช่น "Chumsai Finder Bot")
   - **Channel description**: คำอธิบาย
   - **Category**: เลือกหมวดหมู่
   - **Subcategory**: เลือกหมวดหมู่ย่อย
3. ยอมรับเงื่อนไข
4. คลิก **Create**

### 2.3 ตั้งค่า Channel
1. ไปที่แท็บ **Messaging API**
2. เลื่อนลงหา **Channel access token**
   - คลิก **Issue** → คัดลอก Token (เก็บไว้)
3. เลื่อนลงหา **Channel secret**
   - คลิก **Show** → คัดลอก Secret (เก็บไว้)

---

## 🚀 ขั้นตอนที่ 3: Deploy บน Vercel

### 3.1 เตรียม GitHub Repository
1. สร้าง Repository ใหม่บน GitHub
2. อัปโหลดไฟล์ทั้งหมด:
   - `app.py`
   - `vercel.json`
   - `requirements.txt`
   - `.gitignore`
   - `README.md`

**⚠️ สำคัญ:** ห้ามอัปโหลดไฟล์ `.env` หรือ `.json` (กุญแจ) ขึ้น GitHub!

### 3.2 Import Project ใน Vercel
1. ไปที่ https://vercel.com/
2. คลิก **Add New** → **Project**
3. เลือก Repository จาก GitHub
4. คลิก **Import**

### 3.3 ตั้งค่า Environment Variables
ใน Vercel คลิก **Environment Variables** แล้วเพิ่ม:

**1. LINE_CHANNEL_ACCESS_TOKEN**
```
ค่า: (วาง Token ที่คัดลอกจาก LINE)
```

**2. LINE_CHANNEL_SECRET**
```
ค่า: (วาง Secret ที่คัดลอกจาก LINE)
```

**3. GOOGLE_SHEET_ID**
```
ค่า: (คัดลอกจาก URL ของ Google Sheets)
ตัวอย่าง: https://docs.google.com/spreadsheets/d/1ABC...XYZ/edit
         → เอาส่วน 1ABC...XYZ
```

**4. GOOGLE_CREDENTIALS_JSON**
```
ค่า: (เปิดไฟล์ .json แล้ว copy ทั้งหมด วางเป็น 1 บรรทัด)
```

### 3.4 Deploy
1. คลิก **Deploy**
2. รอสักครู่
3. เมื่อเสร็จจะได้ URL (เช่น `https://your-project.vercel.app`)

---

## 🔗 ขั้นตอนที่ 4: เชื่อมต่อ Webhook

### 4.1 ทดสอบ URL
1. เปิดเบราว์เซอร์ไปที่ `https://your-project.vercel.app/`
2. ต้องเห็นข้อความ: **"LINE Bot Chumsai Finder is running! ✅"**

### 4.2 ตั้งค่า Webhook URL
1. กลับไปที่ LINE Developers Console
2. ไปที่ Channel → แท็บ **Messaging API**
3. เลื่อนลงหา **Webhook settings**
4. คลิก **Edit** แล้วใส่:
   ```
   https://your-project.vercel.app/api/webhook
   ```
5. คลิก **Update**
6. เปิด **Use webhook** → ON
7. คลิก **Verify** → ต้องขึ้น **Success**

### 4.3 ปิด Auto-reply
1. เลื่อนลงหา **Auto-reply messages**
2. คลิก **Edit**
3. ปิด **Auto-reply messages** → OFF
4. **Save**

---

## ✅ ขั้นตอนที่ 5: ทดสอบ

### 5.1 เพิ่มเพื่อน
1. ในหน้า Channel → แท็บ **Messaging API**
2. เลื่อนลงหา QR Code
3. สแกน QR Code ด้วย LINE เพื่อเพิ่มเพื่อน

### 5.2 ทดสอบส่งพิกัด
1. เปิดแชทกับบอท
2. กดปุ่ม **+** → **ตำแหน่งที่อยู่**
3. เลือก **ตำแหน่งปัจจุบัน** หรือค้นหาสถานที่
4. ส่งพิกัดให้บอท
5. บอทจะตอบกลับชุมสายใกล้เคียง!

---

## 🐛 แก้ไขปัญหาที่พบบ่อย

### ❌ บอทไม่ตอบ
1. ตรวจสอบ Webhook Verify → ต้องเป็น Success
2. ดู Logs ใน Vercel:
   - ไปที่ Vercel Dashboard
   - เลือก Project → **Functions**
   - คลิก **View Logs**
3. ตรวจสอบ Environment Variables ใน Vercel

### ❌ Error: Invalid signature
- ตรวจสอบ `LINE_CHANNEL_SECRET` ถูกต้องหรือไม่

### ❌ Error: Unauthorized
- ตรวจสอบ `LINE_CHANNEL_ACCESS_TOKEN` ถูกต้องหรือไม่

### ❌ ไม่เจอข้อมูล Google Sheets
1. ตรวจสอบแชร์ไฟล์ให้ Service Account แล้วหรือยัง
2. ตรวจสอบ `GOOGLE_SHEET_ID` ถูกต้องหรือไม่
3. ตรวจสอบชื่อคอลัมน์ว่าเป็น `chumsa`, `lat`, `lng`

### ❌ ชุมสายไม่ครบ
- ตรวจสอบว่าแต่ละแถวมีข้อมูลครบทุกคอลัมน์
- ตรวจสอบค่า `lat`, `lng` เป็นตัวเลขทศนิยม ไม่มีตัวอักษรปนอยู่

---

## 📞 ต้องการความช่วยเหลือ?

หากมีปัญหา:
1. ตรวจสอบ Logs ใน Vercel ก่อน
2. ทดสอบ Webhook Verify ใน LINE Developers
3. เปิด Issue ใน GitHub Repository

---

**สำเร็จแล้ว! 🎉**
